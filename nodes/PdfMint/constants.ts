/**
 * The margin PDFMint applies when a request sends none, mirrored here so the
 * node can fill in the edges the operator did not override. The API's own
 * default lives in src/options.js as DEFAULT_MARGIN; the two must agree.
 */
export const DEFAULT_MARGIN = '12mm';
